type User {
    id: Int
    name: String
    email: String
    role: String
}

type Product {
    id: Int
    name: String
    price: Int
    amount: Int
    description: String
}

type Error {
    code: Int
    message: String
}